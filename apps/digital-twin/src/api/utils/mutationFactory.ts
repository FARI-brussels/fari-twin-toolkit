/**
 * Mutation factories — promoted into the toolkit as `@fari-brussels/query-vue`.
 * Re-exported here so existing `@/api/utils/mutationFactory` imports keep working
 * while the single implementation lives in the shared package.
 */
export { createMutation, createOptimisticMutation } from '@fari-brussels/query-vue';
