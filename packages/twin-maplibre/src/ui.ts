/**
 * FARI brand UI components, brought in by the bundle so they ship with one
 * install. Tokens + icons ride along transitively.
 *
 * ```ts
 * import { FButton, FChoroplethLegend } from '@fari-brussels/twin-maplibre/ui'
 * ```
 *
 * Remember the stylesheet: `import '@fari-brussels/twin-ui-tokens/css'` (brand tokens) and
 * `import '@fari-brussels/twin-ui-vue/style.css'` (component styles).
 */
export * from '@fari-brussels/twin-ui-vue'
